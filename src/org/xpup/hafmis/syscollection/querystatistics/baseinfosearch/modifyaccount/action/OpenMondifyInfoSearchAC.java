/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package org.xpup.hafmis.syscollection.querystatistics.baseinfosearch.modifyaccount.action;
import java.util.HashMap;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.xpup.common.util.Pagination;
import org.xpup.hafmis.syscollection.querystatistics.baseinfosearch.modifyaccount.form.OpenMondifyInfoAF;
public class OpenMondifyInfoSearchAC extends Action {
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		OpenMondifyInfoAF info = (OpenMondifyInfoAF) form;
    request.getSession().removeAttribute(ShowOpenMondifyListAC.SHOWOPENMONDIFY_KEY);
    Pagination pag = getPagination(ShowOpenMondifyListAC.SHOWOPENMONDIFY_KEY,request);
    pag.getQueryCriterions().put("orgId", info.getOrgId().trim());
    pag.getQueryCriterions().put("orgName", info.getOrgName().trim());
    pag.getQueryCriterions().put("start", info.getStart().trim());
    pag.getQueryCriterions().put("end", info.getEnd().trim());
		return new ActionForward("/showOpenMondifyListAC.do");
	}
  private Pagination getPagination(String paginationKey,HttpServletRequest request) {
    Pagination pagination = (Pagination) request.getSession().getAttribute(paginationKey);
    if (pagination == null) {
      pagination = new Pagination(0, 10, 1, "b.org.id","DESC",new HashMap(0));
      request.getSession().setAttribute(paginationKey, pagination);
    }   
    return pagination;
  }
}