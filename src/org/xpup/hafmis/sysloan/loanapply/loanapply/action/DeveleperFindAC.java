/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package org.xpup.hafmis.sysloan.loanapply.loanapply.action;

import java.util.HashMap;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.xpup.common.util.Pagination;
import org.xpup.hafmis.sysloan.loanapply.loanapply.form.DeveleperNewAF;

/**
 * MyEclipse Struts Creation date: 10-06-2007 XDoclet definition:
 * 
 * @struts.action path="/develeperfindAC" name="develepernewAF" scope="request"
 *                validate="true"
 */
public class DeveleperFindAC extends Action {

  public ActionForward execute(ActionMapping mapping, ActionForm form,
      HttpServletRequest request, HttpServletResponse response) {
    DeveleperNewAF develepernewAF = (DeveleperNewAF) form;

    HashMap criterions = makeCriterionsMap(develepernewAF);

    Pagination pagination = new Pagination(0, 10, 1, "p5.developer_id", "DESC",
        criterions);
    String paginationKey = DeveleperShowAC.PAGINATION_KEY;
    request.setAttribute("fromFind", "0");
    request.getSession().setAttribute(paginationKey, pagination);
    return mapping.findForward("develepershowAC");
  }

  protected HashMap makeCriterionsMap(DeveleperNewAF develepernewAF) {
    HashMap map = new HashMap();
    String developername = develepernewAF.getDevelpername();
    if (developername != null && !"".equals(developername)) {
      map.put("developername", developername.trim());
    }
    return map;
  }
}